#include "main.h"
/**
 * _isupper - prints uppercase letters
 * @c: char
 * Return: returns 0 on succesful execution
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{

		return (1);
	}

	else
	return (0);
}
