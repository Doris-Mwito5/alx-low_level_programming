#include <stdio.h>

/**
 *main - Main function of the program
 *
 *Return: Returns zero when the program executes successfully
 */
int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	return (0);
}
