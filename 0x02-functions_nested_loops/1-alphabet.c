#include "main.h"
#include <stdio.h>
/**
 * main - main function of the program
 *
 * Return: returns 0 when the program executes successfully
 */
int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar(x);
	return (0);
}
