#include <stdio.h>
/**
 *main - main function off the program
 *
 *Return: returns 0 when the program executes successfully
 */
int main(void)
{
	int m;

	for (m = '0'; m <= '9'; m++)
	{
		putchar(m);
		if (d != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
