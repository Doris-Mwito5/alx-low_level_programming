#include "main.h"
/**
 *_abs - computes the absolute value of an integer
 * @c: the init to check
 * Retutn: returns 0 or absolute value
 */
int _abs(int)
{
	if (c < 0)
	{
		init abs_val;

		abs_val = c = -1;
		return(abs_val);
	}
	return (c);
}
